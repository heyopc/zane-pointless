{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      "$room": {
        ".read": "$room.length >= 24 && $room.length <= 64",
        ".write": "$room.length >= 24 && $room.length <= 64"
      }
    }
  }
}